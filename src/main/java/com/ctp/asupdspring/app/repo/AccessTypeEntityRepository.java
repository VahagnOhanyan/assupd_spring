package com.ctp.asupdspring.app.repo;

import com.ctp.asupdspring.domain.AccessTypeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccessTypeEntityRepository extends JpaRepository<AccessTypeEntity, Integer> {
}